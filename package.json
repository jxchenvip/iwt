{
  "name": "iwt",
  "version": "1.0.8",
  "description": "iwt is a javascript utility that returns the value passed in when the passed variable is the value of the type you want, otherwise it returns the value of the default type.",
  "main": "index.js",
  "preferGlobal": true,
  "scripts": {
    "test": "jest --coverage"
  },
  "keywords": [
    "iwt",
    "getValue",
    "typeOf"
  ],
  "author": "jxchen",
  "license": "ISC",
  "devDependencies": {
    "jest": "^24.9.0"
  }
}
